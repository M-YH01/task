# 인수인계 보완 (B 작성)

HANDOFF.md(SHA-256 784da868…3709a)는 문서 규칙대로 A 원본을 그대로 두고, B가 실제로 막힌 지점을 **문서 누락**으로 여기에 적습니다. 다음 인계 때는 이 내용을 HANDOFF.md 새 판에 합치고 manifest 해시를 다시 만드세요.

| # | 문서 누락 | B에게 생긴 일 | 보완할 내용 |
|---|---|---|---|
| 1 | 파일 받는 대체 경로 없음 | 웹 주소 3개가 네트워크 정책(403)으로 막혀 약 5분 멈추고 사용자 재요청이 1회 더 필요했음 | "주소를 못 열면 첨부 파일이나 로컬 경로로 받는다"를 명시하고, 대체 경로를 함께 적기 |
| 2 | source.zip 자체의 해시 없음 | 문서 해시로는 zip이 맞는 파일인지 풀기 전에 확인할 수 없었음 | source.zip SHA-256 기록. B가 받은 값: f52874dfccf859cafdb0e2b7c810f8ae8a7694550a0bf9ab7ff6d6385873c313 |
| 3 | Node 24 확보 방법 없음 | B 환경은 Node 22였음. npm의 node-linux-x64@24 패키지로 24.21.0을 받아 해결 | 버전 확인 명령(`node --version`)과 설치 방법 한 줄 추가 |
| 4 | 문서 명령의 폴더 위치 불명확 | `cd ../ai-b-work` 후 `node tests/run.cjs`인데, SUBMISSION.md는 `node ../ai-b-work/tests/run.cjs`로 달라 헷갈림 | 두 문서의 명령을 하나로 맞추기 |
| 5 | git 기록 미포함 | zip에 .git이 없어 B가 종료 버전을 커밋으로 고정할 수 없었음 | git bundle을 같이 주거나 "B는 해시로 종료 버전을 고정한다"를 명시 |
| 6 | 브라우저 확인 방법·기준 없음 | B가 Playwright 스크립트를 직접 만들어 확인함 | 확인할 사례(정상, PNG 포함, 손상 JSON, 디코딩 실패, 연속 복원)와 합격 기준 적기. B 스크립트: tools/b-browser-check.cjs |
| 7 | 호출 수 세는 기준 모호 | 셸 안에서 부른 npm·node를 1회씩 셀지 불분명. B는 직접 부른 도구만 셈 | PROTOCOL.md의 호출 정의에 예시 추가 |
| 8 | A의 서비스·모델 정보 없음 | "A와 다른 서비스·모델"인지 B가 확인할 수 없었음(이후 사용자 확인으로 해소) | 이름을 가려 두되, 사용자가 따로 보관하는 대응표 위치 적기 |
